{
  "name": "predictobics",
  "private": true,
  "description": "Monorepo root — Vercel: use repo root as project root, set BACKEND_ORIGIN to your FastAPI URL, or REACT_APP_API_URL at build time. See .env.example.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
